/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Game a tournament is played in. **Discriminants are a stable wire contract** —
 * indexers and the SAS game-identity schema lookup (`protocol_config.sas_schemas`)
 * index on these values, so never reorder or renumber. `Manual` (0) needs no
 * identity attestation; every other variant requires a SAS attestation at
 * `join_tournament`. Phase 1 only accepts `Manual` + `Dota2` at create-time
 * (the rest are reserved so the field stays positionally fixed).
 */
export enum SupportedGame {
  Manual,
  Dota2,
  Cs2Faceit,
  Valorant,
  LoL,
}

export type SupportedGameArgs = SupportedGame;

export function getSupportedGameEncoder(): FixedSizeEncoder<SupportedGameArgs> {
  return getEnumEncoder(SupportedGame);
}

export function getSupportedGameDecoder(): FixedSizeDecoder<SupportedGame> {
  return getEnumDecoder(SupportedGame);
}

export function getSupportedGameCodec(): FixedSizeCodec<
  SupportedGameArgs,
  SupportedGame
> {
  return combineCodec(getSupportedGameEncoder(), getSupportedGameDecoder());
}
