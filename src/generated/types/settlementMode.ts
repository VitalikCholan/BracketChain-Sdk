/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Who may report match results for a tournament. Locked at create-time.
 * `OrganizerOnly` (0) is the MVP behaviour and the only mode with a live
 * reporting path today; `PlayerReported` + `Oracle` ship their reporting
 * instructions in later V1 stages of this same redeploy. The enum ships now so
 * `tournament.settlement_mode` is positionally stable across the redeploy.
 */
export enum SettlementMode {
  OrganizerOnly,
  PlayerReported,
  Oracle,
}

export type SettlementModeArgs = SettlementMode;

export function getSettlementModeEncoder(): FixedSizeEncoder<SettlementModeArgs> {
  return getEnumEncoder(SettlementMode);
}

export function getSettlementModeDecoder(): FixedSizeDecoder<SettlementMode> {
  return getEnumDecoder(SettlementMode);
}

export function getSettlementModeCodec(): FixedSizeCodec<
  SettlementModeArgs,
  SettlementMode
> {
  return combineCodec(getSettlementModeEncoder(), getSettlementModeDecoder());
}
