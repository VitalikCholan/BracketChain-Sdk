/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Bracket topology a tournament runs. **Discriminants are a stable wire
 * contract** — never reorder or renumber. `SingleElim` (0) is deliberately the
 * zero variant: Borsh zero-fill on pre-R15 accounts (and `realloc(_, true)`
 * migrations) deserializes as `SingleElim`, which is what every existing
 * tournament actually is.
 *
 * R15 schema-prep: only the field + enum ship in Phase 1 so formats Phase A
 * (Round Robin) / B (Double Elim) / C (Swiss) never need a breaking redeploy.
 * `create_tournament` rejects everything but `SingleElim` with
 * `FormatNotYetSupported` until each phase lifts its gate.
 */
export enum TournamentFormat {
  SingleElim,
  DoubleElim,
  Swiss,
  RoundRobin,
}

export type TournamentFormatArgs = TournamentFormat;

export function getTournamentFormatEncoder(): FixedSizeEncoder<TournamentFormatArgs> {
  return getEnumEncoder(TournamentFormat);
}

export function getTournamentFormatDecoder(): FixedSizeDecoder<TournamentFormat> {
  return getEnumDecoder(TournamentFormat);
}

export function getTournamentFormatCodec(): FixedSizeCodec<
  TournamentFormatArgs,
  TournamentFormat
> {
  return combineCodec(
    getTournamentFormatEncoder(),
    getTournamentFormatDecoder(),
  );
}
