/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";

export const REVEAL_SEED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  196, 119, 194, 112, 156, 211, 239, 105,
]);

export function getRevealSeedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REVEAL_SEED_DISCRIMINATOR);
}

export type RevealSeedInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountTournament extends string | AccountMeta<string> = string,
  TAccountRandomnessAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountTournament extends string
        ? WritableAccount<TAccountTournament>
        : TAccountTournament,
      TAccountRandomnessAccount extends string
        ? ReadonlyAccount<TAccountRandomnessAccount>
        : TAccountRandomnessAccount,
      ...TRemainingAccounts,
    ]
  >;

export type RevealSeedInstructionData = { discriminator: ReadonlyUint8Array };

export type RevealSeedInstructionDataArgs = {};

export function getRevealSeedInstructionDataEncoder(): FixedSizeEncoder<RevealSeedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVEAL_SEED_DISCRIMINATOR }),
  );
}

export function getRevealSeedInstructionDataDecoder(): FixedSizeDecoder<RevealSeedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevealSeedInstructionDataCodec(): FixedSizeCodec<
  RevealSeedInstructionDataArgs,
  RevealSeedInstructionData
> {
  return combineCodec(
    getRevealSeedInstructionDataEncoder(),
    getRevealSeedInstructionDataDecoder(),
  );
}

export type RevealSeedInput<
  TAccountPayer extends string = string,
  TAccountTournament extends string = string,
  TAccountRandomnessAccount extends string = string,
> = {
  /** Permissionless fee-payer — anyone may push the reveal through. */
  payer: TransactionSigner<TAccountPayer>;
  tournament: Address<TAccountTournament>;
  /** `RandomnessAccountData` below. */
  randomnessAccount: Address<TAccountRandomnessAccount>;
};

export function getRevealSeedInstruction<
  TAccountPayer extends string,
  TAccountTournament extends string,
  TAccountRandomnessAccount extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: RevealSeedInput<
    TAccountPayer,
    TAccountTournament,
    TAccountRandomnessAccount
  >,
  config?: { programAddress?: TProgramAddress },
): RevealSeedInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountTournament,
  TAccountRandomnessAccount
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    tournament: { value: input.tournament ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("randomnessAccount", accounts.randomnessAccount),
    ],
    data: getRevealSeedInstructionDataEncoder().encode({}),
    programAddress,
  } as RevealSeedInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTournament,
    TAccountRandomnessAccount
  >);
}

export type ParsedRevealSeedInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless fee-payer — anyone may push the reveal through. */
    payer: TAccountMetas[0];
    tournament: TAccountMetas[1];
    /** `RandomnessAccountData` below. */
    randomnessAccount: TAccountMetas[2];
  };
  data: RevealSeedInstructionData;
};

export function parseRevealSeedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealSeedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      tournament: getNextAccount(),
      randomnessAccount: getNextAccount(),
    },
    data: getRevealSeedInstructionDataDecoder().decode(instruction.data),
  };
}
