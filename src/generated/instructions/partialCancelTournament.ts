/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";

export const PARTIAL_CANCEL_TOURNAMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([194, 241, 74, 25, 111, 229, 230, 141]);

export function getPartialCancelTournamentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PARTIAL_CANCEL_TOURNAMENT_DISCRIMINATOR,
  );
}

export type PartialCancelTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountTournament extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? ReadonlySignerAccount<TAccountOrganizer> &
            AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountTournament extends string
        ? WritableAccount<TAccountTournament>
        : TAccountTournament,
      ...TRemainingAccounts,
    ]
  >;

export type PartialCancelTournamentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PartialCancelTournamentInstructionDataArgs = {};

export function getPartialCancelTournamentInstructionDataEncoder(): FixedSizeEncoder<PartialCancelTournamentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: PARTIAL_CANCEL_TOURNAMENT_DISCRIMINATOR,
    }),
  );
}

export function getPartialCancelTournamentInstructionDataDecoder(): FixedSizeDecoder<PartialCancelTournamentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPartialCancelTournamentInstructionDataCodec(): FixedSizeCodec<
  PartialCancelTournamentInstructionDataArgs,
  PartialCancelTournamentInstructionData
> {
  return combineCodec(
    getPartialCancelTournamentInstructionDataEncoder(),
    getPartialCancelTournamentInstructionDataDecoder(),
  );
}

export type PartialCancelTournamentInput<
  TAccountOrganizer extends string = string,
  TAccountTournament extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  tournament: Address<TAccountTournament>;
};

export function getPartialCancelTournamentInstruction<
  TAccountOrganizer extends string,
  TAccountTournament extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: PartialCancelTournamentInput<TAccountOrganizer, TAccountTournament>,
  config?: { programAddress?: TProgramAddress },
): PartialCancelTournamentInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountTournament
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: false },
    tournament: { value: input.tournament ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("organizer", accounts.organizer),
      getAccountMeta("tournament", accounts.tournament),
    ],
    data: getPartialCancelTournamentInstructionDataEncoder().encode({}),
    programAddress,
  } as PartialCancelTournamentInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountTournament
  >);
}

export type ParsedPartialCancelTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    tournament: TAccountMetas[1];
  };
  data: PartialCancelTournamentInstructionData;
};

export function parsePartialCancelTournamentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPartialCancelTournamentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { organizer: getNextAccount(), tournament: getNextAccount() },
    data: getPartialCancelTournamentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
