/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_RESULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  7, 96, 132, 38, 128, 145, 133, 242,
]);

export function getProposeResultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_RESULT_DISCRIMINATOR,
  );
}

export type ProposeResultInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountProposer extends string | AccountMeta<string> = string,
  TAccountTournament extends string | AccountMeta<string> = string,
  TAccountMatchAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProposer extends string
        ? ReadonlySignerAccount<TAccountProposer> &
            AccountSignerMeta<TAccountProposer>
        : TAccountProposer,
      TAccountTournament extends string
        ? ReadonlyAccount<TAccountTournament>
        : TAccountTournament,
      TAccountMatchAccount extends string
        ? WritableAccount<TAccountMatchAccount>
        : TAccountMatchAccount,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeResultInstructionData = {
  discriminator: ReadonlyUint8Array;
  proposedWinner: Address;
};

export type ProposeResultInstructionDataArgs = { proposedWinner: Address };

export function getProposeResultInstructionDataEncoder(): FixedSizeEncoder<ProposeResultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proposedWinner", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_RESULT_DISCRIMINATOR }),
  );
}

export function getProposeResultInstructionDataDecoder(): FixedSizeDecoder<ProposeResultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proposedWinner", getAddressDecoder()],
  ]);
}

export function getProposeResultInstructionDataCodec(): FixedSizeCodec<
  ProposeResultInstructionDataArgs,
  ProposeResultInstructionData
> {
  return combineCodec(
    getProposeResultInstructionDataEncoder(),
    getProposeResultInstructionDataDecoder(),
  );
}

export type ProposeResultInput<
  TAccountProposer extends string = string,
  TAccountTournament extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  proposer: TransactionSigner<TAccountProposer>;
  tournament: Address<TAccountTournament>;
  matchAccount: Address<TAccountMatchAccount>;
  proposedWinner: ProposeResultInstructionDataArgs["proposedWinner"];
};

export function getProposeResultInstruction<
  TAccountProposer extends string,
  TAccountTournament extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: ProposeResultInput<
    TAccountProposer,
    TAccountTournament,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeResultInstruction<
  TProgramAddress,
  TAccountProposer,
  TAccountTournament,
  TAccountMatchAccount
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    proposer: { value: input.proposer ?? null, isWritable: false },
    tournament: { value: input.tournament ?? null, isWritable: false },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("proposer", accounts.proposer),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("matchAccount", accounts.matchAccount),
    ],
    data: getProposeResultInstructionDataEncoder().encode(
      args as ProposeResultInstructionDataArgs,
    ),
    programAddress,
  } as ProposeResultInstruction<
    TProgramAddress,
    TAccountProposer,
    TAccountTournament,
    TAccountMatchAccount
  >);
}

export type ParsedProposeResultInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    proposer: TAccountMetas[0];
    tournament: TAccountMetas[1];
    matchAccount: TAccountMetas[2];
  };
  data: ProposeResultInstructionData;
};

export function parseProposeResultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeResultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      proposer: getNextAccount(),
      tournament: getNextAccount(),
      matchAccount: getNextAccount(),
    },
    data: getProposeResultInstructionDataDecoder().decode(instruction.data),
  };
}
