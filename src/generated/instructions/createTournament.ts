/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findProtocolConfigPda,
  findTournamentPda,
  findVaultPda,
} from "../pdas";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";
import {
  getPayoutPresetDecoder,
  getPayoutPresetEncoder,
  type PayoutPreset,
  type PayoutPresetArgs,
} from "../types";

export const CREATE_TOURNAMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([158, 137, 233, 231, 73, 132, 191, 68]);

export function getCreateTournamentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_TOURNAMENT_DISCRIMINATOR,
  );
}

export type CreateTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountTournament extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountOrganizerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountRent extends string | AccountMeta<string> =
    "SysvarRent111111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? WritableSignerAccount<TAccountOrganizer> &
            AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountTournament extends string
        ? WritableAccount<TAccountTournament>
        : TAccountTournament,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountOrganizerTokenAccount extends string
        ? WritableAccount<TAccountOrganizerTokenAccount>
        : TAccountOrganizerTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountRent extends string
        ? ReadonlyAccount<TAccountRent>
        : TAccountRent,
      ...TRemainingAccounts,
    ]
  >;

export type CreateTournamentInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  entryFee: bigint;
  maxParticipants: number;
  payoutPreset: PayoutPreset;
  registrationDeadline: bigint;
  organizerDeposit: bigint;
};

export type CreateTournamentInstructionDataArgs = {
  name: string;
  entryFee: number | bigint;
  maxParticipants: number;
  payoutPreset: PayoutPresetArgs;
  registrationDeadline: number | bigint;
  organizerDeposit: number | bigint;
};

export function getCreateTournamentInstructionDataEncoder(): Encoder<CreateTournamentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["entryFee", getU64Encoder()],
      ["maxParticipants", getU16Encoder()],
      ["payoutPreset", getPayoutPresetEncoder()],
      ["registrationDeadline", getI64Encoder()],
      ["organizerDeposit", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_TOURNAMENT_DISCRIMINATOR }),
  );
}

export function getCreateTournamentInstructionDataDecoder(): Decoder<CreateTournamentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["entryFee", getU64Decoder()],
    ["maxParticipants", getU16Decoder()],
    ["payoutPreset", getPayoutPresetDecoder()],
    ["registrationDeadline", getI64Decoder()],
    ["organizerDeposit", getU64Decoder()],
  ]);
}

export function getCreateTournamentInstructionDataCodec(): Codec<
  CreateTournamentInstructionDataArgs,
  CreateTournamentInstructionData
> {
  return combineCodec(
    getCreateTournamentInstructionDataEncoder(),
    getCreateTournamentInstructionDataDecoder(),
  );
}

export type CreateTournamentAsyncInput<
  TAccountOrganizer extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTokenMint extends string = string,
  TAccountTournament extends string = string,
  TAccountVault extends string = string,
  TAccountOrganizerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountRent extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  /**
   * SPL Token mint for the tournament's prize pool. Any valid SPL mint is
   * accepted (USDC, wSOL for native-SOL tournaments via wrap, custom).
   * Frontend gatekeeps user-facing token selection — on-chain trusts the
   * caller. `Account<Mint>` validates the account is a real Mint.
   */
  tokenMint: Address<TAccountTokenMint>;
  /**
   * Tournament PDA. `name` is used directly as a seed; capped at
   * `MAX_TOURNAMENT_NAME_LEN` (32) bytes — Solana's per-seed limit. Length
   * validated in the handler before account init.
   */
  tournament?: Address<TAccountTournament>;
  vault?: Address<TAccountVault>;
  /**
   * Optional organizer ATA used to fund `organizer_deposit`. Required when
   * `organizer_deposit > 0`; pass `None` to skip. Mint + owner constraints
   * guarantee the deposit is debited from the organizer's own funds in the
   * configured tournament token. Anchor 0.32 auto-unwraps `Option<Account>`
   * inside constraint expressions and skips the check when the account is
   * `None`, so we reference fields directly without explicit Option handling.
   */
  organizerTokenAccount?: Address<TAccountOrganizerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  rent?: Address<TAccountRent>;
  name: CreateTournamentInstructionDataArgs["name"];
  entryFee: CreateTournamentInstructionDataArgs["entryFee"];
  maxParticipants: CreateTournamentInstructionDataArgs["maxParticipants"];
  payoutPreset: CreateTournamentInstructionDataArgs["payoutPreset"];
  registrationDeadline: CreateTournamentInstructionDataArgs["registrationDeadline"];
  organizerDeposit: CreateTournamentInstructionDataArgs["organizerDeposit"];
};

export async function getCreateTournamentInstructionAsync<
  TAccountOrganizer extends string,
  TAccountProtocolConfig extends string,
  TAccountTokenMint extends string,
  TAccountTournament extends string,
  TAccountVault extends string,
  TAccountOrganizerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountRent extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: CreateTournamentAsyncInput<
    TAccountOrganizer,
    TAccountProtocolConfig,
    TAccountTokenMint,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountRent
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateTournamentInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountProtocolConfig,
    TAccountTokenMint,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountRent
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    tournament: { value: input.tournament ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    organizerTokenAccount: {
      value: input.organizerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    rent: { value: input.rent ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }
  if (!accounts.tournament.value) {
    accounts.tournament.value = await findTournamentPda({
      organizer: getAddressFromResolvedInstructionAccount(
        "organizer",
        accounts.organizer.value,
      ),
      name: getNonNullResolvedInstructionInput("name", args.name),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      tournament: getAddressFromResolvedInstructionAccount(
        "tournament",
        accounts.tournament.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.rent.value) {
    accounts.rent.value =
      "SysvarRent111111111111111111111111111111111" as Address<"SysvarRent111111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("organizer", accounts.organizer),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("organizerTokenAccount", accounts.organizerTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("rent", accounts.rent),
    ],
    data: getCreateTournamentInstructionDataEncoder().encode(
      args as CreateTournamentInstructionDataArgs,
    ),
    programAddress,
  } as CreateTournamentInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountProtocolConfig,
    TAccountTokenMint,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountRent
  >);
}

export type CreateTournamentInput<
  TAccountOrganizer extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountTokenMint extends string = string,
  TAccountTournament extends string = string,
  TAccountVault extends string = string,
  TAccountOrganizerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountRent extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  protocolConfig: Address<TAccountProtocolConfig>;
  /**
   * SPL Token mint for the tournament's prize pool. Any valid SPL mint is
   * accepted (USDC, wSOL for native-SOL tournaments via wrap, custom).
   * Frontend gatekeeps user-facing token selection — on-chain trusts the
   * caller. `Account<Mint>` validates the account is a real Mint.
   */
  tokenMint: Address<TAccountTokenMint>;
  /**
   * Tournament PDA. `name` is used directly as a seed; capped at
   * `MAX_TOURNAMENT_NAME_LEN` (32) bytes — Solana's per-seed limit. Length
   * validated in the handler before account init.
   */
  tournament: Address<TAccountTournament>;
  vault: Address<TAccountVault>;
  /**
   * Optional organizer ATA used to fund `organizer_deposit`. Required when
   * `organizer_deposit > 0`; pass `None` to skip. Mint + owner constraints
   * guarantee the deposit is debited from the organizer's own funds in the
   * configured tournament token. Anchor 0.32 auto-unwraps `Option<Account>`
   * inside constraint expressions and skips the check when the account is
   * `None`, so we reference fields directly without explicit Option handling.
   */
  organizerTokenAccount?: Address<TAccountOrganizerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  rent?: Address<TAccountRent>;
  name: CreateTournamentInstructionDataArgs["name"];
  entryFee: CreateTournamentInstructionDataArgs["entryFee"];
  maxParticipants: CreateTournamentInstructionDataArgs["maxParticipants"];
  payoutPreset: CreateTournamentInstructionDataArgs["payoutPreset"];
  registrationDeadline: CreateTournamentInstructionDataArgs["registrationDeadline"];
  organizerDeposit: CreateTournamentInstructionDataArgs["organizerDeposit"];
};

export function getCreateTournamentInstruction<
  TAccountOrganizer extends string,
  TAccountProtocolConfig extends string,
  TAccountTokenMint extends string,
  TAccountTournament extends string,
  TAccountVault extends string,
  TAccountOrganizerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountRent extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: CreateTournamentInput<
    TAccountOrganizer,
    TAccountProtocolConfig,
    TAccountTokenMint,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountRent
  >,
  config?: { programAddress?: TProgramAddress },
): CreateTournamentInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountProtocolConfig,
  TAccountTokenMint,
  TAccountTournament,
  TAccountVault,
  TAccountOrganizerTokenAccount,
  TAccountTokenProgram,
  TAccountSystemProgram,
  TAccountRent
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    tournament: { value: input.tournament ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    organizerTokenAccount: {
      value: input.organizerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    rent: { value: input.rent ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.rent.value) {
    accounts.rent.value =
      "SysvarRent111111111111111111111111111111111" as Address<"SysvarRent111111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("organizer", accounts.organizer),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("organizerTokenAccount", accounts.organizerTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("rent", accounts.rent),
    ],
    data: getCreateTournamentInstructionDataEncoder().encode(
      args as CreateTournamentInstructionDataArgs,
    ),
    programAddress,
  } as CreateTournamentInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountProtocolConfig,
    TAccountTokenMint,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountRent
  >);
}

export type ParsedCreateTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    protocolConfig: TAccountMetas[1];
    /**
     * SPL Token mint for the tournament's prize pool. Any valid SPL mint is
     * accepted (USDC, wSOL for native-SOL tournaments via wrap, custom).
     * Frontend gatekeeps user-facing token selection — on-chain trusts the
     * caller. `Account<Mint>` validates the account is a real Mint.
     */
    tokenMint: TAccountMetas[2];
    /**
     * Tournament PDA. `name` is used directly as a seed; capped at
     * `MAX_TOURNAMENT_NAME_LEN` (32) bytes — Solana's per-seed limit. Length
     * validated in the handler before account init.
     */
    tournament: TAccountMetas[3];
    vault: TAccountMetas[4];
    /**
     * Optional organizer ATA used to fund `organizer_deposit`. Required when
     * `organizer_deposit > 0`; pass `None` to skip. Mint + owner constraints
     * guarantee the deposit is debited from the organizer's own funds in the
     * configured tournament token. Anchor 0.32 auto-unwraps `Option<Account>`
     * inside constraint expressions and skips the check when the account is
     * `None`, so we reference fields directly without explicit Option handling.
     */
    organizerTokenAccount?: TAccountMetas[5] | undefined;
    tokenProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
    rent: TAccountMetas[8];
  };
  data: CreateTournamentInstructionData;
};

export function parseCreateTournamentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateTournamentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === BRACKET_CHAIN_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organizer: getNextAccount(),
      protocolConfig: getNextAccount(),
      tokenMint: getNextAccount(),
      tournament: getNextAccount(),
      vault: getNextAccount(),
      organizerTokenAccount: getNextOptionalAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      rent: getNextAccount(),
    },
    data: getCreateTournamentInstructionDataDecoder().decode(instruction.data),
  };
}
