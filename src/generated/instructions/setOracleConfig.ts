/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProtocolConfigPda } from "../pdas";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";

export const SET_ORACLE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([96, 171, 6, 98, 153, 183, 233, 31]);

export function getSetOracleConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_ORACLE_CONFIG_DISCRIMINATOR,
  );
}

export type SetOracleConfigInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetOracleConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  switchboardQueue: Address;
  maxStaleSlots: number;
  minOracleSamples: number;
};

export type SetOracleConfigInstructionDataArgs = {
  switchboardQueue: Address;
  maxStaleSlots: number;
  minOracleSamples: number;
};

export function getSetOracleConfigInstructionDataEncoder(): FixedSizeEncoder<SetOracleConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["switchboardQueue", getAddressEncoder()],
      ["maxStaleSlots", getU32Encoder()],
      ["minOracleSamples", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ORACLE_CONFIG_DISCRIMINATOR }),
  );
}

export function getSetOracleConfigInstructionDataDecoder(): FixedSizeDecoder<SetOracleConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["switchboardQueue", getAddressDecoder()],
    ["maxStaleSlots", getU32Decoder()],
    ["minOracleSamples", getU32Decoder()],
  ]);
}

export function getSetOracleConfigInstructionDataCodec(): FixedSizeCodec<
  SetOracleConfigInstructionDataArgs,
  SetOracleConfigInstructionData
> {
  return combineCodec(
    getSetOracleConfigInstructionDataEncoder(),
    getSetOracleConfigInstructionDataDecoder(),
  );
}

export type SetOracleConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountProtocolConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  switchboardQueue: SetOracleConfigInstructionDataArgs["switchboardQueue"];
  maxStaleSlots: SetOracleConfigInstructionDataArgs["maxStaleSlots"];
  minOracleSamples: SetOracleConfigInstructionDataArgs["minOracleSamples"];
};

export async function getSetOracleConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountProtocolConfig extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: SetOracleConfigAsyncInput<TAccountAuthority, TAccountProtocolConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetOracleConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocolConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await findProtocolConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
    ],
    data: getSetOracleConfigInstructionDataEncoder().encode(
      args as SetOracleConfigInstructionDataArgs,
    ),
    programAddress,
  } as SetOracleConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocolConfig
  >);
}

export type SetOracleConfigInput<
  TAccountAuthority extends string = string,
  TAccountProtocolConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  protocolConfig: Address<TAccountProtocolConfig>;
  switchboardQueue: SetOracleConfigInstructionDataArgs["switchboardQueue"];
  maxStaleSlots: SetOracleConfigInstructionDataArgs["maxStaleSlots"];
  minOracleSamples: SetOracleConfigInstructionDataArgs["minOracleSamples"];
};

export function getSetOracleConfigInstruction<
  TAccountAuthority extends string,
  TAccountProtocolConfig extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: SetOracleConfigInput<TAccountAuthority, TAccountProtocolConfig>,
  config?: { programAddress?: TProgramAddress },
): SetOracleConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountProtocolConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
    ],
    data: getSetOracleConfigInstructionDataEncoder().encode(
      args as SetOracleConfigInstructionDataArgs,
    ),
    programAddress,
  } as SetOracleConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProtocolConfig
  >);
}

export type ParsedSetOracleConfigInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    protocolConfig: TAccountMetas[1];
  };
  data: SetOracleConfigInstructionData;
};

export function parseSetOracleConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetOracleConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), protocolConfig: getNextAccount() },
    data: getSetOracleConfigInstructionDataDecoder().decode(instruction.data),
  };
}
