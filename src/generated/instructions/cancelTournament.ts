/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultPda } from "../pdas";
import { BRACKET_CHAIN_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_TOURNAMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([249, 227, 133, 5, 9, 142, 29, 122]);

export function getCancelTournamentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_TOURNAMENT_DISCRIMINATOR,
  );
}

export type CancelTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountTournament extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountOrganizerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountTournament extends string
        ? WritableAccount<TAccountTournament>
        : TAccountTournament,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountOrganizerTokenAccount extends string
        ? WritableAccount<TAccountOrganizerTokenAccount>
        : TAccountOrganizerTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CancelTournamentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelTournamentInstructionDataArgs = {};

export function getCancelTournamentInstructionDataEncoder(): FixedSizeEncoder<CancelTournamentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_TOURNAMENT_DISCRIMINATOR }),
  );
}

export function getCancelTournamentInstructionDataDecoder(): FixedSizeDecoder<CancelTournamentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelTournamentInstructionDataCodec(): FixedSizeCodec<
  CancelTournamentInstructionDataArgs,
  CancelTournamentInstructionData
> {
  return combineCodec(
    getCancelTournamentInstructionDataEncoder(),
    getCancelTournamentInstructionDataDecoder(),
  );
}

export type CancelTournamentAsyncInput<
  TAccountCaller extends string = string,
  TAccountTournament extends string = string,
  TAccountVault extends string = string,
  TAccountOrganizerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Only required to be the organizer when flipping status to Cancelled
   * before `registration_deadline + ABANDONED_CANCEL_GRACE_SECS`; past that,
   * any signer may flip an abandoned pre-start tournament (H-2 backstop).
   * Once status == Cancelled, any signer can call to process refund chunks.
   */
  caller: TransactionSigner<TAccountCaller>;
  tournament: Address<TAccountTournament>;
  vault?: Address<TAccountVault>;
  /**
   * Organizer's ATA in the tournament's token mint. Required only when an
   * unrefunded `organizer_deposit > 0` is being processed in this call.
   * Constraints (mint + owner) are validated in-handler so that callers
   * processing later refund chunks may pass `None`.
   */
  organizerTokenAccount?: Address<TAccountOrganizerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getCancelTournamentInstructionAsync<
  TAccountCaller extends string,
  TAccountTournament extends string,
  TAccountVault extends string,
  TAccountOrganizerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: CancelTournamentAsyncInput<
    TAccountCaller,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelTournamentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    tournament: { value: input.tournament ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    organizerTokenAccount: {
      value: input.organizerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      tournament: getAddressFromResolvedInstructionAccount(
        "tournament",
        accounts.tournament.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("organizerTokenAccount", accounts.organizerTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getCancelTournamentInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelTournamentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram
  >);
}

export type CancelTournamentInput<
  TAccountCaller extends string = string,
  TAccountTournament extends string = string,
  TAccountVault extends string = string,
  TAccountOrganizerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Only required to be the organizer when flipping status to Cancelled
   * before `registration_deadline + ABANDONED_CANCEL_GRACE_SECS`; past that,
   * any signer may flip an abandoned pre-start tournament (H-2 backstop).
   * Once status == Cancelled, any signer can call to process refund chunks.
   */
  caller: TransactionSigner<TAccountCaller>;
  tournament: Address<TAccountTournament>;
  vault: Address<TAccountVault>;
  /**
   * Organizer's ATA in the tournament's token mint. Required only when an
   * unrefunded `organizer_deposit > 0` is being processed in this call.
   * Constraints (mint + owner) are validated in-handler so that callers
   * processing later refund chunks may pass `None`.
   */
  organizerTokenAccount?: Address<TAccountOrganizerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getCancelTournamentInstruction<
  TAccountCaller extends string,
  TAccountTournament extends string,
  TAccountVault extends string,
  TAccountOrganizerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
>(
  input: CancelTournamentInput<
    TAccountCaller,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CancelTournamentInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountTournament,
  TAccountVault,
  TAccountOrganizerTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BRACKET_CHAIN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    tournament: { value: input.tournament ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    organizerTokenAccount: {
      value: input.organizerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("tournament", accounts.tournament),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("organizerTokenAccount", accounts.organizerTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getCancelTournamentInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelTournamentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountTournament,
    TAccountVault,
    TAccountOrganizerTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedCancelTournamentInstruction<
  TProgram extends string = typeof BRACKET_CHAIN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Only required to be the organizer when flipping status to Cancelled
     * before `registration_deadline + ABANDONED_CANCEL_GRACE_SECS`; past that,
     * any signer may flip an abandoned pre-start tournament (H-2 backstop).
     * Once status == Cancelled, any signer can call to process refund chunks.
     */
    caller: TAccountMetas[0];
    tournament: TAccountMetas[1];
    vault: TAccountMetas[2];
    /**
     * Organizer's ATA in the tournament's token mint. Required only when an
     * unrefunded `organizer_deposit > 0` is being processed in this call.
     * Constraints (mint + owner) are validated in-handler so that callers
     * processing later refund chunks may pass `None`.
     */
    organizerTokenAccount?: TAccountMetas[3] | undefined;
    tokenProgram: TAccountMetas[4];
  };
  data: CancelTournamentInstructionData;
};

export function parseCancelTournamentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelTournamentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === BRACKET_CHAIN_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      tournament: getNextAccount(),
      vault: getNextAccount(),
      organizerTokenAccount: getNextOptionalAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getCancelTournamentInstructionDataDecoder().decode(instruction.data),
  };
}
