/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPayoutPresetDecoder,
  getPayoutPresetEncoder,
  getSettlementModeDecoder,
  getSettlementModeEncoder,
  getSupportedGameDecoder,
  getSupportedGameEncoder,
  getTournamentStatusDecoder,
  getTournamentStatusEncoder,
  type PayoutPreset,
  type PayoutPresetArgs,
  type SettlementMode,
  type SettlementModeArgs,
  type SupportedGame,
  type SupportedGameArgs,
  type TournamentStatus,
  type TournamentStatusArgs,
} from "../types";

export const TOURNAMENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  175, 139, 119, 242, 115, 194, 57, 92,
]);

export function getTournamentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TOURNAMENT_DISCRIMINATOR);
}

export type Tournament = {
  discriminator: ReadonlyUint8Array;
  organizer: Address;
  name: string;
  /**
   * SPL Token mint for the prize pool. Any mint allowed (USDC, wSOL for
   * SOL tournaments, custom). Frontend gatekeeps user-facing selection.
   */
  tokenMint: Address;
  vault: Address;
  entryFee: bigint;
  /**
   * Optional organizer top-up transferred into the vault at creation.
   * `0` is allowed. Treated as a refundable commitment (Variant A):
   * returned to the organizer on `cancel_tournament` (pre-start) AND on
   * `report_result` final-match. The deposit is excluded from the
   * prize-pool basis — protocol fee + placement payouts apply to
   * `vault.amount - organizer_deposit` only.
   */
  organizerDeposit: bigint;
  /**
   * Set true once the deposit has been refunded — by `cancel_tournament`
   * (any-call, idempotent across chunks) or by `report_result` final-match.
   * Independent of per-participant `refund_paid` flags.
   */
  organizerDepositRefunded: boolean;
  maxParticipants: number;
  bracketSize: number;
  participantCount: number;
  matchesInitialized: number;
  matchesReported: number;
  totalMatches: number;
  registrationDeadline: bigint;
  createdAt: bigint;
  startedAt: bigint;
  completedAt: bigint;
  status: TournamentStatus;
  payoutPreset: PayoutPreset;
  seedHash: ReadonlyUint8Array;
  champion: Address;
  bump: number;
  vaultBump: number;
  /** Game played; gates SAS identity requirement at `join_tournament`. */
  game: SupportedGame;
  /** Who may report results. Locked at create-time. */
  settlementMode: SettlementMode;
  /**
   * Dispute window (seconds) for PlayerReported / Oracle settlement. Unused
   * by OrganizerOnly. Wired by the V1 player-reported stage of this redeploy.
   */
  disputeWindowSecs: number;
  /** Switchboard randomness account committed via `request_seed` (VRF stage). */
  vrfRandomnessAccount: Address;
  /** Slot the VRF commitment was made at; `reveal_seed` reads after it passes. */
  vrfCommitSlot: bigint;
  /**
   * True once `reveal_seed` has populated `seed_hash` from VRF. Gates
   * `start_tournament` for non-OrganizerOnly tournaments.
   */
  seedRevealed: boolean;
};

export type TournamentArgs = {
  organizer: Address;
  name: string;
  /**
   * SPL Token mint for the prize pool. Any mint allowed (USDC, wSOL for
   * SOL tournaments, custom). Frontend gatekeeps user-facing selection.
   */
  tokenMint: Address;
  vault: Address;
  entryFee: number | bigint;
  /**
   * Optional organizer top-up transferred into the vault at creation.
   * `0` is allowed. Treated as a refundable commitment (Variant A):
   * returned to the organizer on `cancel_tournament` (pre-start) AND on
   * `report_result` final-match. The deposit is excluded from the
   * prize-pool basis — protocol fee + placement payouts apply to
   * `vault.amount - organizer_deposit` only.
   */
  organizerDeposit: number | bigint;
  /**
   * Set true once the deposit has been refunded — by `cancel_tournament`
   * (any-call, idempotent across chunks) or by `report_result` final-match.
   * Independent of per-participant `refund_paid` flags.
   */
  organizerDepositRefunded: boolean;
  maxParticipants: number;
  bracketSize: number;
  participantCount: number;
  matchesInitialized: number;
  matchesReported: number;
  totalMatches: number;
  registrationDeadline: number | bigint;
  createdAt: number | bigint;
  startedAt: number | bigint;
  completedAt: number | bigint;
  status: TournamentStatusArgs;
  payoutPreset: PayoutPresetArgs;
  seedHash: ReadonlyUint8Array;
  champion: Address;
  bump: number;
  vaultBump: number;
  /** Game played; gates SAS identity requirement at `join_tournament`. */
  game: SupportedGameArgs;
  /** Who may report results. Locked at create-time. */
  settlementMode: SettlementModeArgs;
  /**
   * Dispute window (seconds) for PlayerReported / Oracle settlement. Unused
   * by OrganizerOnly. Wired by the V1 player-reported stage of this redeploy.
   */
  disputeWindowSecs: number;
  /** Switchboard randomness account committed via `request_seed` (VRF stage). */
  vrfRandomnessAccount: Address;
  /** Slot the VRF commitment was made at; `reveal_seed` reads after it passes. */
  vrfCommitSlot: number | bigint;
  /**
   * True once `reveal_seed` has populated `seed_hash` from VRF. Gates
   * `start_tournament` for non-OrganizerOnly tournaments.
   */
  seedRevealed: boolean;
};

/** Gets the encoder for {@link TournamentArgs} account data. */
export function getTournamentEncoder(): Encoder<TournamentArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["organizer", getAddressEncoder()],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["tokenMint", getAddressEncoder()],
      ["vault", getAddressEncoder()],
      ["entryFee", getU64Encoder()],
      ["organizerDeposit", getU64Encoder()],
      ["organizerDepositRefunded", getBooleanEncoder()],
      ["maxParticipants", getU16Encoder()],
      ["bracketSize", getU16Encoder()],
      ["participantCount", getU16Encoder()],
      ["matchesInitialized", getU16Encoder()],
      ["matchesReported", getU16Encoder()],
      ["totalMatches", getU16Encoder()],
      ["registrationDeadline", getI64Encoder()],
      ["createdAt", getI64Encoder()],
      ["startedAt", getI64Encoder()],
      ["completedAt", getI64Encoder()],
      ["status", getTournamentStatusEncoder()],
      ["payoutPreset", getPayoutPresetEncoder()],
      ["seedHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["champion", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["vaultBump", getU8Encoder()],
      ["game", getSupportedGameEncoder()],
      ["settlementMode", getSettlementModeEncoder()],
      ["disputeWindowSecs", getU32Encoder()],
      ["vrfRandomnessAccount", getAddressEncoder()],
      ["vrfCommitSlot", getU64Encoder()],
      ["seedRevealed", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TOURNAMENT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Tournament} account data. */
export function getTournamentDecoder(): Decoder<Tournament> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["organizer", getAddressDecoder()],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["tokenMint", getAddressDecoder()],
    ["vault", getAddressDecoder()],
    ["entryFee", getU64Decoder()],
    ["organizerDeposit", getU64Decoder()],
    ["organizerDepositRefunded", getBooleanDecoder()],
    ["maxParticipants", getU16Decoder()],
    ["bracketSize", getU16Decoder()],
    ["participantCount", getU16Decoder()],
    ["matchesInitialized", getU16Decoder()],
    ["matchesReported", getU16Decoder()],
    ["totalMatches", getU16Decoder()],
    ["registrationDeadline", getI64Decoder()],
    ["createdAt", getI64Decoder()],
    ["startedAt", getI64Decoder()],
    ["completedAt", getI64Decoder()],
    ["status", getTournamentStatusDecoder()],
    ["payoutPreset", getPayoutPresetDecoder()],
    ["seedHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["champion", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["vaultBump", getU8Decoder()],
    ["game", getSupportedGameDecoder()],
    ["settlementMode", getSettlementModeDecoder()],
    ["disputeWindowSecs", getU32Decoder()],
    ["vrfRandomnessAccount", getAddressDecoder()],
    ["vrfCommitSlot", getU64Decoder()],
    ["seedRevealed", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link Tournament} account data. */
export function getTournamentCodec(): Codec<TournamentArgs, Tournament> {
  return combineCodec(getTournamentEncoder(), getTournamentDecoder());
}

export function decodeTournament<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Tournament, TAddress>;
export function decodeTournament<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Tournament, TAddress>;
export function decodeTournament<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Tournament, TAddress> | MaybeAccount<Tournament, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTournamentDecoder(),
  );
}

export async function fetchTournament<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Tournament, TAddress>> {
  const maybeAccount = await fetchMaybeTournament(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTournament<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Tournament, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTournament(maybeAccount);
}

export async function fetchAllTournament(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Tournament>[]> {
  const maybeAccounts = await fetchAllMaybeTournament(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTournament(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Tournament>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTournament(maybeAccount));
}
